Finish the extracted CARVEOUT repository completely as a live GenLayer Studionet release. Read AGENT_HANDOFF.md first and execute it fully; this is an execution goal, not a review task. Preserve the implemented product and its current UI identity. Product: A provider-backed SLA exception protocol: independently prove the miss first, then let GenLayer decide whether a frozen carve-out actually excuses it. Use only chain 61999 and https://studio.genlayer.com/api. Keep generic injected EIP-1193 window.ethereum only; no 61997, Studio-dev, Snaps, WalletConnect, embedded wallet or backend signer. Preserve the current lifecycle: create_agreement → accept_agreement → open_incident → verify_measurement → claim_exception → adjudicate_exception → optional challenge_exception/resolve_challenge → finalize_incident (or bounded default breach) → withdraw_credit. Run the real GenVM linter, all Direct Mode tests, required integration/real-consensus checks and Next.js typecheck/build. Fix genuine failures without weakening substantive validator replay, fail-closed evidence states, frozen rules, challenge mechanics, bounded liveness, accounting or deterministic settlement. Deploy the exact final source on 61999, wait for FINALIZED plus successful execution, verify deployed source/schema and get_stats, wire the exact address into the multipage frontend, publish it, and execute docs/LIVE_DEMO.md with real public evidence, a real positive economic path and at least one honest negative/fail-closed path. Record address, source commit, transaction/explorer proof, test counts and live final state in docs/REVIEW_EVIDENCE.md. Do not fabricate evidence or relax protocol rules to force a demo. Leave CI green, docs synchronized and the repository submission-ready.
