export default function Protocol(){return <section className="shell page"><div className="kicker">Protocol anatomy</div><h1>The exception is the contested object.</h1><p style={{maxWidth:760,fontSize:18,lineHeight:1.6}}>Deterministic systems can establish a measured SLA result. They cannot reliably decide whether a frozen semantic clause such as “an upstream infrastructure failure materially caused this service impact” is established by conflicting public evidence, whether causation matches the observation interval, or whether that exact frozen exception applies. CARVEOUT uses deterministic code for timing, thresholds, interval arithmetic and money, while GenLayer consensus is restricted to the contested semantic interpretation.</p><div className="protocol-flow"><div><b>01</b><p>The provider freezes SLA terms, source policy and exclusions, then bonds the maximum credit.</p></div><div><b>02</b><p>The named customer accepts before the SLA exposure window and may submit a completed miss with independent evidence.</p></div><div><b>03</b><p>Each frozen source is fetched and independently reduced to stable service/window facts. Validators compare exact consequential manifests while ignoring volatile page metadata.</p></div><div><b>04</b><p>GenLayer interprets whether the exact frozen exception and causal interval are established; a bonded challenge can request full-record review.</p></div><div><b>05</b><p>Contract code applies timing, interval validation, liable basis points, GEN allocation, settlement and accounting deterministically.</p></div></div><div className="protocol-boundary"><div className="kicker">Deterministic contract responsibilities</div><p>Agreement formation · authorization · timestamps · source-policy enforcement · threshold comparison · interval validation · partial-liability arithmetic · GEN allocation · settlement · accounting.</p><div className="kicker">GenLayer consensus responsibilities</div><p>Extract and interpret contested public facts · determine whether facts establish the exact frozen exception · assess causation during the measured impact · re-evaluate that semantic judgment on challenge.</p></div></section>}
